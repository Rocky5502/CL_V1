# Paper integration

The full manuscript is maintained in the lab/Overleaf package. This public experiment repository intentionally keeps paper integration lightweight until results are frozen.

Generated tables and figures should be written under `paper/generated/` from `scripts/make_paper_artifacts.py`. Empirical values remain TBD until a frozen run exists.
