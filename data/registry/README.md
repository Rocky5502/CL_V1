# Dataset registry

`configs/datasets.yaml` registers external benchmark and multilingual resources used as source or diagnostic anchors. Raw third-party bytes remain local; immutable revisions and SHA-256 manifests are recorded during snapshotting.
