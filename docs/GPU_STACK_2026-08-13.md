# GPU stack audit — 2026-08-13

Target host: Windows 10 Pro + WSL2, NVIDIA GeForce RTX 5070 Ti, user-reported 48 GB accessible GPU memory pending verification.

Initial bring-up uses Python 3.11, PyTorch 2.12.1 with the CUDA 13.0 wheel, and Transformers 4.51 or newer. Exact working versions are frozen after successful smoke tests.

Verify the host NVIDIA driver with `nvidia-smi`. Under WSL2, use the Windows NVIDIA driver for GPU support and avoid installing a second Linux display driver inside WSL.
