# Security

Do not report API keys in issues. If a credential is accidentally committed, revoke it immediately and rewrite history before public release. Raw model outputs can contain sensitive or harmful text; do not attach unrestricted raw dumps to public issues.
